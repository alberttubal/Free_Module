# Server
PORT=4000
JWT_SECRET=change_me_to_a_random_32_byte_value

# Database (preferred: use a single URL)
DATABASE_URL=postgres://postgres:AcadFreedom123!@localhost:5432/free_module

# Optional flags
NODE_ENV=development
DB_SSL=false
